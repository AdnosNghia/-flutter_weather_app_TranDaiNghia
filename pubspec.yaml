name: flutter_weather_app_trandainghia
description: "Ứng dụng Thời tiết Flutter - Lab 4"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.1
  geolocator: ^12.0.0
  geocoding: ^3.0.0
  provider: ^6.1.2
  shared_preferences: ^2.3.0
  intl: ^0.19.0
  cached_network_image: ^3.3.1
  flutter_dotenv: ^5.1.0
  connectivity_plus: ^6.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
